package Collections;

public class MapTest {
    public static void main(String[] args) {

//        public List<TopologyAttribute> getModerationEnabledAttributes(){
//            return getAttributes().stream()
//                    .filter(attribute -> attribute.isEnabled())
//                    .map(attribute -> TopologyAttribute.getTopologyAttributeFromString(attribute.getName()))
//                    .filter(topologyAttribute -> topologyAttribute != null)
//                    .collect(Collectors.toList());
//        }\

///        updatedTopologyAttributes.removeIf(moderatedAttribute -> !moderationSupportedAttributes.getModerationEnabledAttributes().contains(moderatedAttribute));
    }
}
